@AGENTS.md

# Commits

- Commits are authored by the repo owner only: never add a `Co-Authored-By` trailer or any other AI attribution line to commit messages or PR descriptions.
- Pushing to `main` when a piece of work is done is fine without asking.
